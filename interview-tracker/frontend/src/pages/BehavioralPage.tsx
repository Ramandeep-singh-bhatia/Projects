import TopicPage from './TopicPage';
import { TopicCategory } from '../types';

const BehavioralPage = () => {
  return <TopicPage category={TopicCategory.BEHAVIORAL} />;
};

export default BehavioralPage;
