package com.interviewtracker.model;

public enum DependencyStrength {
    STRONG,   // Must master before advancing
    MEDIUM,   // Should understand before advancing
    WEAK      // Helpful but not critical
}
